//this is an enum: used when a variable should only havea few fixed values
package com.example.ratestaff.model;

public enum RoleType{
    TA,
    PROF,
    INSTRUCTOR,
    STAFF //a staff member can be any of these roles
}
